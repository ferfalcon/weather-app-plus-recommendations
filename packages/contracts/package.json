{
  "name": "@weather-app-plus-recommendations/contracts",
  "private": true,
  "version": "0.0.0",
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./dist/index.js"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "pnpm -C ../.. exec eslint packages/contracts/src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm -C ../.. exec vitest run --dir packages/contracts --passWithNoTests"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
